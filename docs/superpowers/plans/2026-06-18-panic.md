# Panic fake (PANIC) — task plan

Branch `build/16-panic`. Source of truth: `/home/sergey/SecondReality/PANIC/SHUTDOWN.C`.

## Tasks (TDD; pure logic first)

1. **Assets** — copy `MONSTER.U` (64000 B raw 320×200 indices) and `MONSTER.PAL` (768 B 6-bit
   VGA palette) to `apps/lab/public/pics/`; vendor both into `packages/parts/src/panic/__fixtures__`. ✅

2. **`palette.ts` + test** — `parseVgaPalette(bytes)` → `Uint8Array(256*3)` of 6-bit values;
   `paletteLut(palette)` → `Uint8Array(256*4)` sRGB bytes (6-bit ×4, alpha 255). Test byte-exact
   vs the vendored MONSTER.PAL and the ×4 expansion.

3. **`picture.ts` + test** — `MONSTER_W=320`, `MONSTER_H=200`, `parsePicture(bytes)` validating the
   64000-byte raw index buffer. Test length + index range against the fixture.

4. **`crash.ts` + tests** — the pure crash sim (the heart of the port):
   - `CrashState` { phase, frame, fadeLevel, bandHalf, wipeX, dotBright, dotVisible }.
   - `createCrashState()` initial.
   - `stepCrash(state)` advances exactly one frame through the SHUTDOWN.C sequence:
     washes (2) → collapse (11) → wipe (47) → dot (60) → hold.
   - `rasterCrash(out, state, picture)` writes the 320×200 index buffer for the current state:
     squash the picture into the band, the wipe blackout, the centre dot.
   - Tests: frame budget/phase boundaries; collapse band half-height sequence
     (a∈{32,26,21,17,14,11,9,7,5,4,3} → bandHalf in 200-space); fade level monotonic to white;
     wipe X progression 20→160 step 3; dot cosine pulse (1.5 periods / 60 frames); raster stays in
     bounds and only emits valid indices.

5. **`nodes.ts`** — `CrashSurface` (DataTexture index→LUT blit, row-flip, SRGBColorSpace,
   `setFilter`). Mirror dot-tunnel `RasterSurface`.

6. **`panic.ts`** — the `Effect`: `load()` fetch+parse MONSTER.U/.PAL; `init()` build surface;
   fixed-step `update()` advancing the crash; `render()` blit to the supplied target;
   `setMode('authentic'|'modern')`; proper `dispose()`. Self-loop in the lab.

7. **Verify** — `pnpm lint && pnpm typecheck && pnpm test && pnpm build` all green. STATUS doc.

## Notes
- C integer division truncates toward zero; `a*5/6` and `400*b/a` and the fade `(a*63+pal*(64-a))/64`
  are integer ops — match exactly.
- Do not touch `packages/engine/**`, `packages/parts/src/index.ts`, `apps/lab/src/**`, README.
