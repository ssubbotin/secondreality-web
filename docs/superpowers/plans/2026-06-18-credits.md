# Plan — Part #20 "Credits / greetings scroll" (ENDSCRL)

Branch: `build/23-credits`. Source of truth: `/home/sergey/SecondReality/ENDSCRL/MAIN.C` +
`ASMYT.ASM`. Design: `docs/superpowers/specs/2026-06-18-credits-design.md`.

## Steps

1. **Vendor assets + fixtures.** Copy `ENDSCRL/FONA.UH` and `ENDSCRL/ENDSCROL.TXT` to
   `apps/lab/public/pics/`. Vendor the same two into `packages/parts/src/credits/__fixtures__/` for
   byte-exact unit tests.
2. **`decode-u.ts` (+ test).** Port the `.U`/`.UH` decoder (header, 6-bit palette, raw/RLE pixels).
   Oracle: the vendored `FONA.UH` (1500×30, ink ⊆ {0,1,2,3}).
3. **`font.ts` (+ test).** Port `init()` glyph segmentation + `FONA_ORDER` + forced space; `measure`.
4. **`scrolltext.ts` (+ test).** Parse `ENDSCROL.TXT` into lines; oracle against the vendored text.
5. **`layout.ts` (+ test).** `measureLine`, `centerOffset` with C truncation.
6. **`scroll.ts` (+ test).** `scrollAt(frame)` wrap, `rowToLineRow` mapping.
7. **`palette.ts` (+ test).** The exact greyscale ramp from the `setrgbpalette` calls.
8. **`raster.ts` (+ test).** `rasterField` window fill; assert a short line's pixel footprint.
9. **`nodes.ts`.** `RasterSurface` index→LUT DataTexture + `Blit`, `setFilter`.
10. **`credits.ts`.** The `Effect` + `setMode` on the 70 Hz accumulator; render into supplied target.
11. **`index.ts`.** Re-export `Credits` from the part dir (do NOT edit `packages/parts/src/index.ts`).
12. **Verify.** `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` all green.
13. **STATUS doc + commits** with the co-author trailer.

## Notes
- This worktree predates the engine `text/` + `assets/` layer, so the `.U` decode + font + blit are
  self-implemented locally (recorded in the STATUS `deferred` section). Prefer the engine API when
  the branch is rebased onto a commit that has it.
- C int division truncates toward zero — `centerOffset` uses `Math.trunc`.
